# 外部端点接入能力 Implementation Plan

> **Execution note:** 本计划只描述实施步骤；当前 Plan Mode 下不修改文件、不创建文件、不执行会改变项目状态的命令。

**Goal:** 在 `H:\Resources\RTLinux\Demos\MB_DDF_v2` 中恢复 DDS 对“外部端点对象”的接入能力，但不接入任何具体硬件、UDP、SPI、CAN 等端点实现。

**Architecture:** 只在 `src/MB_DDF/DDS` 内增加一个轻量抽象接口，让 `Publisher`/`Subscriber` 能委托外部对象完成 `send/receive`。DDS 共享内存路径保持现状，不引入旧工程的非 DDS 子系统，也不恢复旧目录结构。

**Tech Stack:** C++20、CMake、PowerShell 构建脚本、GoogleTest、AArch64 交叉编译。

---

## 目标

实现后应具备：

- DDS 仍可按当前共享内存模式发布/订阅。
- `Publisher` 可绑定外部端点并把 `publish/write` 委托到外部 `send`。
- `Subscriber` 可绑定外部端点并把 `read` 委托到外部 `receive`。
- 可选提供一个 `ExternalPort` 便捷包装，组合 publisher/subscriber，实现 `write/read`。
- 不引入任何具体端点实现。
- 不引入旧工程其它子系统。
- 不破坏 v2 当前的共享内存超时读取、零拷贝写入、构建脚本和测试脚本。

## 改动范围

新增：

- `src/MB_DDF/DDS/ExternalEndpoint.h`
- `src/MB_DDF/DDS/ExternalPort.h`
- `src/MB_DDF/DDS/ExternalPort.cpp`
- `tests/unit/test_external_endpoint.cpp`

修改：

- `src/MB_DDF/DDS/Publisher.h`
- `src/MB_DDF/DDS/Publisher.cpp`
- `src/MB_DDF/DDS/Subscriber.h`
- `src/MB_DDF/DDS/Subscriber.cpp`
- `src/MB_DDF/DDS/DDSCore.h`
- `src/MB_DDF/DDS/DDSCore.cpp`
- `CMakeLists.txt`
- `tests/CMakeLists.txt`
- `tests/test-dds-only.ps1`
- `docs/guides/DDS_Standalone_使用说明.md`，只补充外部端点抽象用法，不写具体后端。

不做：

- 不创建 `src/MB_DDF/Endpoint`。
- 不接入具体硬件或链路实现。
- 不恢复旧工程兼容别名。
- 不改 host/native 构建约束。

---

## 实施步骤

### Task 1: 增加外部端点抽象接口

**Files:**

- Create: `src/MB_DDF/DDS/ExternalEndpoint.h`

**Step 1.1: 新建接口头文件**

写入接口：

```cpp
#pragma once

#include <cstddef>
#include <cstdint>
#include <memory>

namespace MB_DDF {
namespace DDS {

class ExternalEndpoint {
public:
    virtual ~ExternalEndpoint() = default;

    virtual bool send(const uint8_t* data, size_t size) = 0;
    virtual int32_t receive(uint8_t* data, size_t capacity) = 0;
    virtual int32_t receive(uint8_t* data, size_t capacity, uint32_t timeout_us) = 0;
    virtual size_t mtu() const = 0;

    virtual int control(uint32_t command, void* argument, size_t argument_size) {
        (void)command;
        (void)argument;
        (void)argument_size;
        return -1;
    }
};

using ExternalEndpointRef = std::shared_ptr<ExternalEndpoint>;

} // namespace DDS
} // namespace MB_DDF
```

设计约束：

- `send/receive` 使用 `size_t`，避免 DDS 内部截断。
- `control()` 是可选能力，默认不支持。
- 不包含事件等待接口，避免第一版引入阻塞唤醒复杂度。

---

### Task 2: 让 Publisher 支持外部端点

**Files:**

- Modify: `src/MB_DDF/DDS/Publisher.h`
- Modify: `src/MB_DDF/DDS/Publisher.cpp`

**Step 2.1: 修改 `Publisher.h`**

增加 include：

```cpp
#include "MB_DDF/DDS/ExternalEndpoint.h"
```

修改构造函数签名：

```cpp
Publisher(TopicMetadata* metadata,
          RingBuffer* ring_buffer,
          const std::string& publisher_name = "",
          ExternalEndpointRef external_io = nullptr);
```

增加私有成员：

```cpp
ExternalEndpointRef external_io_;
```

**Step 2.2: 修改 `Publisher.cpp` 构造函数**

```cpp
Publisher::Publisher(TopicMetadata* metadata,
                     RingBuffer* ring_buffer,
                     const std::string& publisher_name,
                     ExternalEndpointRef external_io)
    : metadata_(metadata),
      ring_buffer_(ring_buffer),
      external_io_(std::move(external_io)),
      publisher_name_(publisher_name) {
    std::random_device rd;
    std::mt19937_64 gen(rd());
    publisher_id_ = gen();

    if (publisher_name_.empty()) {
        publisher_name_ = "publisher_" + std::to_string(publisher_id_);
    }
}
```

同时补充：

```cpp
#include <utility>
```

**Step 2.3: 修改 `publish()`**

在 RingBuffer 路径前增加外部端点分支：

```cpp
bool Publisher::publish(const void* data, size_t size) {
    if (external_io_ != nullptr) {
        if (data == nullptr && size > 0) {
            return false;
        }
        return external_io_->send(static_cast<const uint8_t*>(data), size);
    }

    if (ring_buffer_ == nullptr) {
        return false;
    }

    return ring_buffer_->publish_message(data, size);
}
```

保持：

- `write()` 继续调用 `publish()`。
- `begin_message()` 和 `publish_fill()` 仍只支持共享内存 RingBuffer；外部端点模式下因为 `ring_buffer_ == nullptr`，返回 invalid/false。

---

### Task 3: 让 Subscriber 支持外部端点

**Files:**

- Modify: `src/MB_DDF/DDS/Subscriber.h`
- Modify: `src/MB_DDF/DDS/Subscriber.cpp`

**Step 3.1: 修改 `Subscriber.h`**

增加 include：

```cpp
#include "MB_DDF/DDS/ExternalEndpoint.h"
#include <vector>
```

修改构造函数签名：

```cpp
Subscriber(TopicMetadata* metadata,
           RingBuffer* ring_buffer,
           const std::string& subscriber_name = "",
           ExternalEndpointRef external_io = nullptr);
```

增加私有成员：

```cpp
ExternalEndpointRef external_io_{};
std::vector<uint8_t> receive_buffer_{};
```

**Step 3.2: 修改构造函数**

```cpp
Subscriber::Subscriber(TopicMetadata* metadata,
                       RingBuffer* ring_buffer,
                       const std::string& subscriber_name,
                       ExternalEndpointRef external_io)
    : metadata_(metadata),
      ring_buffer_(ring_buffer),
      callback_(nullptr),
      subscribed_(false),
      running_(false),
      worker_thread_(),
      external_io_(std::move(external_io)),
      subscriber_name_(subscriber_name) {
    std::random_device rd;
    std::mt19937_64 gen(rd());
    subscriber_id_ = gen();

    if (subscriber_name_.empty()) {
        subscriber_name_ = "subscriber_" + std::to_string(subscriber_id_);
    }

    if (external_io_ != nullptr) {
        const size_t capacity = external_io_->mtu();
        receive_buffer_.resize(capacity > 0 ? capacity : 1);
    }
}
```

补充：

```cpp
#include <utility>
```

**Step 3.3: 修改 `subscribe()`**

逻辑：

```cpp
bool Subscriber::subscribe(MessageCallback callback) {
    if (subscribed_.load()) {
        LOG_DEBUG << "Subscriber " << subscriber_id_ << " " << subscriber_name_ << " already subscribed";
        return false;
    }

    if (external_io_ == nullptr) {
        if (ring_buffer_ == nullptr) {
            LOG_ERROR << "Subscriber " << subscriber_id_ << " " << subscriber_name_ << " ring buffer is null";
            return false;
        }

        subscriber_state_ = ring_buffer_->register_subscriber(subscriber_id_, subscriber_name_);
        if (!subscriber_state_) {
            LOG_DEBUG << "Failed to register subscriber " << subscriber_id_ << " " << subscriber_name_;
            return false;
        }
    }

    callback_ = callback;
    subscribed_.store(true);
    running_.store(callback_ != nullptr);

    if (callback_) {
        LOG_DEBUG << "Subscriber " << subscriber_id_ << " " << subscriber_name_ << " subscribed with callback";
        worker_thread_ = std::thread(&Subscriber::worker_loop, this);
    }

    LOG_DEBUG << "Subscriber " << subscriber_id_ << " " << subscriber_name_ << " subscribed successfully";
    return true;
}
```

**Step 3.4: 修改 `worker_loop()`**

在现有 RingBuffer 逻辑前插入外部端点分支：

```cpp
void Subscriber::worker_loop() {
    while (running_.load()) {
        if (external_io_ != nullptr) {
            const int32_t n = external_io_->receive(
                receive_buffer_.data(),
                receive_buffer_.size(),
                static_cast<uint32_t>(10000));

            if (n > 0 && callback_) {
                callback_(receive_buffer_.data(), static_cast<size_t>(n), 0);
            } else if (n == 0) {
                std::this_thread::sleep_for(std::chrono::milliseconds(1));
            }

            continue;
        }

        // 保留现有 RingBuffer worker_loop 逻辑
    }
}
```

注意：

- 外部端点回调模式用 10ms receive timeout，避免析构时 join 长时间卡住。
- 时间戳传 `0`，因为外部端点抽象不定义时间戳来源。

**Step 3.5: 修改 `read(void*, size_t, bool)`**

在 RingBuffer 路径前插入：

```cpp
if (external_io_ != nullptr) {
    const int32_t ret = external_io_->receive(static_cast<uint8_t*>(data), size);
    return ret > 0 ? static_cast<size_t>(ret) : 0;
}
```

保留已有规则：

```cpp
if (!subscribed_.load()) return 0;
if (callback_) return 0;
```

**Step 3.6: 修改 `read(void*, size_t, uint32_t)`**

在共享内存超时读取前插入：

```cpp
if (external_io_ != nullptr) {
    return external_io_->receive(static_cast<uint8_t*>(data), size, timeout_us);
}
```

保留 v2 当前 DDS 共享内存超时读取逻辑，不回退成旧行为。

---

### Task 4: 增加 DDSCore 外部端点创建重载

**Files:**

- Modify: `src/MB_DDF/DDS/DDSCore.h`
- Modify: `src/MB_DDF/DDS/DDSCore.cpp`

**Step 4.1: 修改 `DDSCore.h`**

新增发布者重载：

```cpp
std::shared_ptr<Publisher> create_publisher(const std::string& topic_name,
                                            ExternalEndpointRef external_io);

std::shared_ptr<Publisher> create_writer(const std::string& topic_name,
                                         ExternalEndpointRef external_io);
```

新增订阅者重载：

```cpp
std::shared_ptr<Subscriber> create_subscriber(const std::string& topic_name,
                                              ExternalEndpointRef external_io,
                                              const MessageCallback& callback = nullptr);

std::shared_ptr<Subscriber> create_reader(const std::string& topic_name,
                                          ExternalEndpointRef external_io,
                                          const MessageCallback& callback = nullptr);
```

**Step 4.2: 修改 `DDSCore.cpp`**

新增实现：

```cpp
std::shared_ptr<Publisher> DDSCore::create_publisher(const std::string& topic_name,
                                                     ExternalEndpointRef external_io) {
    if (external_io == nullptr) {
        LOG_ERROR << "failed to create external publisher, external endpoint is null, topic name: " << topic_name;
        return nullptr;
    }

    if (process_name_.empty()) {
        process_name_ = get_process_name();
    }

    LOG_DEBUG << "created external publisher, topic name: " << topic_name;
    return std::make_shared<Publisher>(nullptr, nullptr, process_name_, std::move(external_io));
}

std::shared_ptr<Publisher> DDSCore::create_writer(const std::string& topic_name,
                                                  ExternalEndpointRef external_io) {
    return create_publisher(topic_name, std::move(external_io));
}

std::shared_ptr<Subscriber> DDSCore::create_subscriber(const std::string& topic_name,
                                                       ExternalEndpointRef external_io,
                                                       const MessageCallback& callback) {
    if (external_io == nullptr) {
        LOG_ERROR << "failed to create external subscriber, external endpoint is null, topic name: " << topic_name;
        return nullptr;
    }

    if (process_name_.empty()) {
        process_name_ = get_process_name();
    }

    LOG_DEBUG << "created external subscriber, topic name: " << topic_name;
    auto subscriber = std::make_shared<Subscriber>(nullptr, nullptr, process_name_, std::move(external_io));
    if (!subscriber->subscribe(callback)) {
        return nullptr;
    }
    return subscriber;
}

std::shared_ptr<Subscriber> DDSCore::create_reader(const std::string& topic_name,
                                                   ExternalEndpointRef external_io,
                                                   const MessageCallback& callback) {
    return create_subscriber(topic_name, std::move(external_io), callback);
}
```

补充：

```cpp
#include <utility>
```

设计要求：

- 外部端点模式不调用 `initialize()`。
- 外部端点模式不创建共享内存 Topic。
- `topic_name` 仅用于日志和 API 一致性。

---

### Task 5: 增加 ExternalPort 便捷包装

**Files:**

- Create: `src/MB_DDF/DDS/ExternalPort.h`
- Create: `src/MB_DDF/DDS/ExternalPort.cpp`

**Step 5.1: 新建 `ExternalPort.h`**

```cpp
#pragma once

#include <cstddef>
#include <cstdint>
#include <memory>

#include "MB_DDF/DDS/DDSCore.h"
#include "MB_DDF/DDS/ExternalEndpoint.h"
#include "MB_DDF/DDS/Publisher.h"
#include "MB_DDF/DDS/Subscriber.h"

namespace MB_DDF {
namespace DDS {

class ExternalPort {
public:
    explicit ExternalPort(ExternalEndpointRef external_io, MessageCallback callback = nullptr);

    bool write(const void* data, size_t size);
    size_t read(void* data, size_t size);
    int32_t read(void* data, size_t size, uint32_t timeout_us);

    ExternalEndpointRef external() const { return external_io_; }

    std::shared_ptr<Publisher> publisher;
    std::shared_ptr<Subscriber> subscriber;

private:
    ExternalEndpointRef external_io_;
};

} // namespace DDS
} // namespace MB_DDF
```

**Step 5.2: 新建 `ExternalPort.cpp`**

```cpp
#include "MB_DDF/DDS/ExternalPort.h"

#include <utility>

namespace MB_DDF {
namespace DDS {

ExternalPort::ExternalPort(ExternalEndpointRef external_io, MessageCallback callback)
    : external_io_(std::move(external_io)) {
    if (!external_io_) {
        return;
    }

    auto& dds_core = DDSCore::instance();
    publisher = dds_core.create_publisher("external://publisher", external_io_);
    subscriber = dds_core.create_subscriber("external://subscriber", external_io_, callback);
}

bool ExternalPort::write(const void* data, size_t size) {
    return publisher && publisher->write(data, size);
}

size_t ExternalPort::read(void* data, size_t size) {
    return subscriber ? subscriber->read(data, size) : 0;
}

int32_t ExternalPort::read(void* data, size_t size, uint32_t timeout_us) {
    return subscriber ? subscriber->read(data, size, timeout_us) : 0;
}

} // namespace DDS
} // namespace MB_DDF
```

---

### Task 6: 更新构建文件

**Files:**

- Modify: `CMakeLists.txt`
- Modify: `tests/CMakeLists.txt`

**Step 6.1: 修改根 `CMakeLists.txt`**

在 `DDS_CORE_SOURCES` 增加：

```cmake
src/MB_DDF/DDS/ExternalPort.cpp
```

在 `DDS_PUBLIC_HEADERS` 增加：

```cmake
src/MB_DDF/DDS/ExternalEndpoint.h
src/MB_DDF/DDS/ExternalPort.h
```

**Step 6.2: 修改 `tests/CMakeLists.txt`**

在 `TEST_SOURCES` 增加：

```cmake
${CMAKE_CURRENT_SOURCE_DIR}/unit/test_external_endpoint.cpp
```

---

### Task 7: 增加单元测试

**Files:**

- Create: `tests/unit/test_external_endpoint.cpp`

**Step 7.1: 写 FakeExternalEndpoint**

测试文件核心结构：

```cpp
#include <algorithm>
#include <array>
#include <atomic>
#include <chrono>
#include <cstring>
#include <memory>
#include <string>
#include <thread>
#include <vector>

#include <gtest/gtest.h>

#include "MB_DDF/DDS/DDSCore.h"
#include "MB_DDF/DDS/ExternalEndpoint.h"
#include "MB_DDF/DDS/ExternalPort.h"
#include "MB_DDF/DDS/Publisher.h"
#include "MB_DDF/DDS/Subscriber.h"

using namespace MB_DDF::DDS;

namespace {

class FakeExternalEndpoint : public ExternalEndpoint {
public:
    bool send(const uint8_t* data, size_t size) override {
        if (data == nullptr && size > 0) {
            return false;
        }
        last_tx.assign(data, data + size);
        return send_ok;
    }

    int32_t receive(uint8_t* data, size_t capacity) override {
        ++receive_calls;
        const size_t copy_size = std::min(capacity, next_rx.size());
        if (copy_size > 0) {
            std::memcpy(data, next_rx.data(), copy_size);
            next_rx.erase(next_rx.begin(), next_rx.begin() + static_cast<std::ptrdiff_t>(copy_size));
        }
        return static_cast<int32_t>(copy_size);
    }

    int32_t receive(uint8_t* data, size_t capacity, uint32_t timeout_us) override {
        last_timeout_us = timeout_us;
        return receive(data, capacity);
    }

    size_t mtu() const override {
        return mtu_value;
    }

    bool send_ok{true};
    size_t mtu_value{256};
    uint32_t last_timeout_us{0};
    std::atomic<uint32_t> receive_calls{0};
    std::vector<uint8_t> last_tx{};
    std::vector<uint8_t> next_rx{};
};

} // namespace
```

**Step 7.2: 测 Publisher 委托发送**

```cpp
TEST(ExternalEndpointTest, PublisherWriteDelegatesToExternalSend) {
    auto external = std::make_shared<FakeExternalEndpoint>();
    Publisher publisher(nullptr, nullptr, "external_pub", external);

    const char payload[] = "external-payload";
    ASSERT_TRUE(publisher.write(payload, sizeof(payload)));

    ASSERT_EQ(external->last_tx.size(), sizeof(payload));
    EXPECT_EQ(std::memcmp(external->last_tx.data(), payload, sizeof(payload)), 0);
}
```

**Step 7.3: 测外部模式不支持零拷贝 RingBuffer 写入**

```cpp
TEST(ExternalEndpointTest, PublisherExternalModeDoesNotExposeRingBufferWriteSlot) {
    auto external = std::make_shared<FakeExternalEndpoint>();
    Publisher publisher(nullptr, nullptr, "external_pub", external);

    auto message = publisher.begin_message(128);
    EXPECT_FALSE(message.valid());

    const bool filled = publisher.publish_fill(128, [](void*, size_t) -> size_t {
        return 1;
    });
    EXPECT_FALSE(filled);
}
```

**Step 7.4: 测 Subscriber 非超时读取**

```cpp
TEST(ExternalEndpointTest, SubscriberReadDelegatesToExternalReceive) {
    auto external = std::make_shared<FakeExternalEndpoint>();
    external->next_rx = {'A', 'B', 'C'};

    Subscriber subscriber(nullptr, nullptr, "external_sub", external);
    ASSERT_TRUE(subscriber.subscribe(nullptr));

    std::array<char, 8> buffer{};
    const size_t n = subscriber.read(buffer.data(), buffer.size());

    EXPECT_EQ(n, 3u);
    EXPECT_EQ(std::string(buffer.data(), 3), "ABC");
    EXPECT_EQ(external->receive_calls.load(), 1u);

    subscriber.unsubscribe();
}
```

**Step 7.5: 测 Subscriber 超时读取参数透传**

```cpp
TEST(ExternalEndpointTest, SubscriberTimedReadDelegatesTimeout) {
    auto external = std::make_shared<FakeExternalEndpoint>();
    external->next_rx = {'D', 'E'};

    Subscriber subscriber(nullptr, nullptr, "external_sub", external);
    ASSERT_TRUE(subscriber.subscribe(nullptr));

    std::array<char, 8> buffer{};
    const int32_t n = subscriber.read(buffer.data(), buffer.size(), static_cast<uint32_t>(250));

    EXPECT_EQ(n, 2);
    EXPECT_EQ(external->last_timeout_us, 250u);
    EXPECT_EQ(std::string(buffer.data(), 2), "DE");

    subscriber.unsubscribe();
}
```

**Step 7.6: 测 DDSCore 外部创建重载**

```cpp
TEST(ExternalEndpointTest, DDSCoreCreatesExternalPublisherAndSubscriber) {
    auto external = std::make_shared<FakeExternalEndpoint>();

    auto& dds = DDSCore::instance();
    auto publisher = dds.create_publisher("external://tx", external);
    auto subscriber = dds.create_subscriber("external://rx", external);

    ASSERT_NE(publisher, nullptr);
    ASSERT_NE(subscriber, nullptr);

    const char payload[] = "core-external";
    EXPECT_TRUE(publisher->write(payload, sizeof(payload)));
    EXPECT_EQ(external->last_tx.size(), sizeof(payload));

    external->next_rx = {'O', 'K'};
    std::array<char, 8> buffer{};
    EXPECT_EQ(subscriber->read(buffer.data(), buffer.size()), 2u);
    EXPECT_EQ(std::string(buffer.data(), 2), "OK");
}
```

**Step 7.7: 测 ExternalPort 包装**

```cpp
TEST(ExternalPortTest, WriteAndReadUseExternalPublisherAndSubscriber) {
    auto external = std::make_shared<FakeExternalEndpoint>();
    ExternalPort port(external);

    const char payload[] = "port-write";
    ASSERT_TRUE(port.write(payload, sizeof(payload)));
    ASSERT_EQ(external->last_tx.size(), sizeof(payload));

    external->next_rx = {'R', 'X'};
    std::array<char, 8> buffer{};
    EXPECT_EQ(port.read(buffer.data(), buffer.size()), 2u);
    EXPECT_EQ(std::string(buffer.data(), 2), "RX");
}
```

**Step 7.8: 测回调模式**

```cpp
TEST(ExternalEndpointTest, SubscriberCallbackReceivesExternalData) {
    auto external = std::make_shared<FakeExternalEndpoint>();
    external->next_rx = {'C', 'B'};

    Subscriber subscriber(nullptr, nullptr, "external_callback_sub", external);

    std::atomic<int> callback_count{0};
    ASSERT_TRUE(subscriber.subscribe([&](const void* data, size_t size, uint64_t timestamp) {
        EXPECT_EQ(timestamp, 0u);
        ASSERT_NE(data, nullptr);
        EXPECT_EQ(size, 2u);
        callback_count.fetch_add(1, std::memory_order_relaxed);
    }));

    std::this_thread::sleep_for(std::chrono::milliseconds(50));
    subscriber.unsubscribe();

    EXPECT_GE(callback_count.load(), 1);
}
```

---

### Task 8: 更新测试脚本默认过滤器

**Files:**

- Modify: `tests/test-dds-only.ps1`

**Step 8.1: 扩展默认 `TestFilter`**

把参数默认值改为包含新测试：

```powershell
[string]$TestFilter = "Message*:*RingBuffer*:*SharedMemory*:*TopicRegistry*:*PubSub*:*DDSCore*:*SemaphoreGuard*:*ExternalEndpoint*:*ExternalPort*",
```

保持：

- 参数名不变。
- 输出格式不变。
- 原静态检查保留。

---

### Task 9: 更新文档

**Files:**

- Modify: `docs/guides/DDS_Standalone_使用说明.md`

**Step 9.1: 增加短节**

建议加在 API 使用说明后：

```markdown
## 外部端点抽象

DDS 还提供一个轻量外部端点抽象，用于把已有收发对象接到 `Publisher` / `Subscriber`。

头文件：

```cpp
#include "MB_DDF/DDS/ExternalEndpoint.h"
#include "MB_DDF/DDS/ExternalPort.h"
```

外部端点只需要实现：

- `send(const uint8_t*, size_t)`
- `receive(uint8_t*, size_t)`
- `receive(uint8_t*, size_t, uint32_t timeout_us)`
- `mtu()`

示例：

```cpp
auto external = std::make_shared<MyExternalEndpoint>();

auto& dds = MB_DDF::DDS::DDSCore::instance();
auto writer = dds.create_publisher("external://tx", external);
auto reader = dds.create_subscriber("external://rx", external);

writer->write(data, size);
auto n = reader->read(buffer, sizeof(buffer), 500000);
```

说明：

- 外部端点模式不创建共享内存 Topic。
- `begin_message()` / `publish_fill()` 仍只用于 DDS 共享内存 RingBuffer。
- 回调模式下不要再手动 `read()`。
```

---

## 验证步骤

### 验证 1: 只做交叉编译测试构建

运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\test-dds-only.ps1 -BuildOnly
```

预期：

- 退出码 `0`
- 静态检查通过
- 生成 `build\aarch64\Debug\tests\MB_DDF_v2_Tests`
- 不出现旧工程目录恢复

### 验证 2: 目标板执行新增测试

目标板可达后运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\test-dds-only.ps1 -TestFilter "ExternalEndpoint*:*ExternalPort*"
```

预期：

- 上传 `MB_DDF_v2_Tests`
- gtest 输出中新增测试全部通过
- 退出码 `0`

### 验证 3: 回归 DDS-only 全量默认测试

运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\test-dds-only.ps1
```

预期：

- 默认过滤器包含原测试和新测试
- 原 `Message*`、`RingBuffer*`、`SharedMemory*`、`TopicRegistry*`、`PubSub*`、`DDSCore*`、`SemaphoreGuard*` 仍通过
- 新 `ExternalEndpoint*`、`ExternalPort*` 通过

### 验证 4: 库构建和安装头文件

运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1 lib_debug
```

预期：

- 退出码 `0`
- `install\include\MB_DDF\DDS\ExternalEndpoint.h` 存在
- `install\include\MB_DDF\DDS\ExternalPort.h` 存在
- `install\libs` 下生成静态库/动态库

### 验证 5: 确认没有恢复旧目录

运行：

```powershell
Test-Path .\src\MB_DDF\Endpoint
```

预期：

```text
False
```

---

## 风险和处理

- **旧文件不能直接覆盖 v2 文件。**  
  v2 的 `Publisher`/`Subscriber` 已经有写锁、DDS 超时读取等改动；只能按差异移植外部端点分支。

- **第一版没有真实端点运行验证。**  
  只能通过 `FakeExternalEndpoint` 验证委托行为；真实硬件或链路后端应另做独立适配计划。

- **回调线程退出依赖 receive timeout。**  
  外部回调模式使用 10ms 超时读取，避免析构长时间卡住。未来如果接入阻塞式真实端点，需要保证 timeout 生效。

- **API 不兼容旧工程便捷别名。**  
  第一版使用 `DDS::ExternalEndpoint` / `DDS::ExternalPort`，不恢复旧兼容别名，避免把 v2 拉回旧工程结构。

- **`nullptr` 重载可能有歧义。**  
  调用外部端点重载时应传明确的 `ExternalEndpointRef` 对象，不写 `create_publisher("x", nullptr)`。
